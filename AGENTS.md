# AGENTS.md

본 문서는 현재 프로젝트 전용 Antigravity 지침이다.

## Project Rules
- 기술 스택, 커스텀 명령어, 아키텍처 규칙을 정의한다.
- 디자인 작업 시 프로젝트 루트 `DESIGN.md` (1순위) 또는 전역 `DESIGN.md` (2순위)를 참조한다.


## Superpowers Workflow Enabled
This project uses Obra Superpowers skills (`.agents/skills/`). Always invoke relevant skills before implementation.


## gstack Workflow Enabled
This project uses garrytan gstack skills (`.agents/skills/`). Use `/gstack` or specific gstack skills (`office-hours`, `spec`, `plan-ceo-review`, `review`, `investigate`, `qa`, `ship`, `health`) during project development.


## gstack Workflow Enabled
This project uses garrytan gstack skills (`.agents/skills/`). Use `/gstack` or specific gstack skills (`office-hours`, `spec`, `plan-ceo-review`, `review`, `investigate`, `qa`, `ship`, `health`) during project development.


## Matt Pocock Skills Enabled
This project includes Matt Pocock skills (`.agents/skills/`). Use skills like `code-review`, `codebase-design`, `diagnosing-bugs`, `domain-modeling`, `research`, `tdd`, `ask-matt` during development.
